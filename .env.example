# Open Web Calendar URLs (comma-separated, plain calendar URLs)
# Supports multiple calendar URLs
# These will be encrypted in the HTML output if ENCRYPTION_METHOD=fernet
CALENDAR_URL=https://calendar.google.com/calendar/ical/example%40gmail.com/public/basic.ics,https://calendar.google.com/calendar/ical/another%40gmail.com/public/basic.ics

# Encryption method: 'fernet' or leave empty for no encryption
# If 'fernet', calendar URLs will be encrypted using Fernet encryption
ENCRYPTION_METHOD=fernet

# Fernet encryption key (base64url-encoded 32-byte key)
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('base64').replace(/\+/g, '-').replace(/\//g, '_').replace(/=/g, ''))"
# This key must match the ENCRYPTION_KEY secret in your Cloudflare Worker
ENCRYPTION_KEY=

# Cloudflare Worker URL (optional)
# If using a Cloudflare Worker to decrypt fernet:// URLs server-side, set this to your worker URL
# Example: https://cal-proxy.yourdomain.com
# If not set, defaults to https://open-web-calendar.hosted.quelltext.eu/calendar.html
WORKER_URL=

# Pre-encrypted calendar URLs (optional)
# If you have already encrypted URLs, you can set them here instead of CALENDAR_URL
# This is useful if you want to store encrypted URLs separately
# CALENDAR_URL_ENCRYPTED=fernet://...,fernet://...
